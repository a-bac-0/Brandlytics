# Brandlytics Environment Configuration

# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_key_here
BUCKET_CROPS=crops

# Model Configuration
MODEL_PATH=models/best2.pt

# Hugging Face Hub Configuration
HF_TOKEN=your_huggingface_token_here
HF_ORG=CV-Brandlytics
HF_MODEL_REPO=CV-Brandlytics/ModelM
USE_HF_MODEL=false
HF_CACHE_DIR=models/hf_cache

# Video Processing Configuration
VIDEO_FPS_SAMPLE=1
VIDEO_MAX_FRAMES=0
VIDEO_UPLOAD_DIR=data/raw/videos/input
VIDEO_OUTPUT_DIR=data/raw/videos/output

# ByteTracker Configuration
TRACKER_FRAME_RATE=30
TRACKER_TRACK_THRESH=0.5
TRACKER_TRACK_BUFFER=30
TRACKER_MATCH_THRESH=0.8

# Legacy Configuration (keeping for compatibility)
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=brand_detection
DB_USER=postgres
DB_PASSWORD=your_password_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
SECRET_KEY=your_secret_key_here

# Model Configuration (legacy)
CONFIDENCE_THRESHOLD=0.5

# Cloud Storage (Optional)
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_BUCKET_NAME=your_bucket_name

# Weights & Biases (Optional)
WANDB_API_KEY=your_wandb_key
WANDB_PROJECT=brand-detection-cv

# Docker Configuration
COMPOSE_PROJECT_NAME=brand_detection